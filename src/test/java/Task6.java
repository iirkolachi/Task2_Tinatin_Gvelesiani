public class Task6 {
    public static void main (String[] args) {
        double radiusi = 3.7;
        double fartobi = Math.PI * Math.pow(radiusi,2); //რადგან ^ ეს ნიშანი ხარისხში ასაყვანად double-თან არ გამოიყენება, გამოვიყენე Math.pow
        System.out.println(fartobi);
    }
}
